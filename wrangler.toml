name = "precious-list"
type = "javascript"
compatibility_date = "2024-11-01"
node_compat = true

# For Cloudflare Pages (serverless functions)
[build]
command = "npm install"
cwd = "./"

# Environment variables for production
[env.production]
vars = { NODE_ENV = "production" }
